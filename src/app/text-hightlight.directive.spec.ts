import { TextHightlightDirective } from './text-hightlight.directive';

describe('TextHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
